# Errors in the implemantations

- [ ] In `TodoService.findById` `id` is assumed to be a number and it won't work. A workaround is to use `parseInt(id)`
- [x] In "Form Handling" it wasn't explained how to use `TaskCreator`
